/**
 * Caption for a fence with no info string.
 * Matches SSG `parseCodeMeta` (`""` → `"text"`). Desk shows this as a
 * placeholder only — it is not written back into the markdown source.
 */
export const UNLABELED_CODE_LANGUAGE = 'text'
