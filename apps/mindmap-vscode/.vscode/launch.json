{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "运行 TNotes Mindmap 插件",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/fixtures/workspace"
      ],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "构建插件"
    }
  ]
}
